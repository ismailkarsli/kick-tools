{
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --outfile=dist/index.js && cp manifest.json dist/manifest.json && cp -r icons dist && zip -r -j -FS kick-tools.zip dist/*",
    "dev": "tsc -w"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}